{
  "name": "doctorsofficeapp",
  "version": "1.0.0",
  "private": true,
  "description": "Doctors Office — Clinic Management Platform for Indian Doctors",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:yjs\"",
    "dev:client": "cd client && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:yjs": "cd yjs-server && npm run dev",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "install:all": "cd client && npm install && cd ../server && npm install && cd ../yjs-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
